pipeline{
    agent any
    stages{
        stage("Build"){

        // timeout(time :60,unit : 'SECONDS')
            input {
                message "Should we continue"
                ok "Yes"
                submitter "Mohan"
            }
        steps{
           echo "Input option"
        }
        }
    }
}
